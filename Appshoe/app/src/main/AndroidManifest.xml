<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appshoe">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appshoe"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.Thongtinkhachhang"></activity>
        <activity android:name=".activity.Giohang" />
        <activity android:name=".activity.Chitietsanpham" />
        <activity android:name=".activity.ThongTin" />
        <activity android:name=".activity.LienHe" />
        <activity android:name=".activity.Giaythethao" />
        <activity android:name=".activity.Giaythoitrang" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>